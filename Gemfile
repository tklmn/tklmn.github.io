source "https://rubygems.org"

# Mirrors the gems GitHub Pages uses to build the site, so local builds match
# production. Run `bundle install`, then `bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins
